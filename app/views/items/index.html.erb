<h1>All Complaints</h1>

<%= link_to "New Complaint", new_item_path %>

<% @items.each do |item| %>
  <div>
    <h2><%= item.name %></h2>

    <%= link_to "More Details", item_path(item) %>
  </div>
<% end %>